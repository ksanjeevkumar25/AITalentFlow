@model List<LoginApp.Models.EmployeeSkills>
@{
    ViewData["Title"] = "Admin View";
    var allSkills = ViewBag.AllSkills as List<LoginApp.Models.Skill>;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Login App</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
        }
        .admin-container {
            background: white;
            border-radius: 15px;
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.1);
            margin-top: 50px;
            padding: 40px;
            max-width: 700px;
        }
        .admin-header {
            text-align: center;
            margin-bottom: 40px;
        }
        .admin-icon {
            font-size: 4rem;
            color: #dc3545;
            margin-bottom: 20px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-12">
                <div class="admin-container">
                    <div class="admin-header">
                        <div class="admin-icon">
                            <i class="fas fa-user-shield"></i>
                        </div>
                        <h1>Welcome, Admin!</h1>
                        <p class="text-muted">Pending Skills for Employee 101</p>
                    </div>
                    @if (TempData["UpdateMessage"] != null)
                    {
                        <div class="alert alert-info text-center">@TempData["UpdateMessage"]</div>
                    }
                    @if (Model != null && Model.Count > 0)
                    {
                        <table class="table table-bordered">
                            <thead>
                                <tr>
                                    <th>Skill</th>
                                    <th>Employee Rated Level</th>
                                    <th>Years of Exp</th>
                                    <th>Rate as Supervisor</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var skill in Model)
                                {
                                    <tr>
                                        <td>@(allSkills?.FirstOrDefault(s => s.SkillID == skill.SkillID)?.SkillName ?? skill.SkillID.ToString())</td>
                                        <td>@skill.EmployeeRatedSkillLevel</td>
                                        <td>@skill.YearsOfExperience</td>
                                        <td>
                                            <form asp-action="UpdateSupervisorRating" asp-controller="Home" method="post" class="d-flex align-items-center">
                                                <input type="hidden" name="employeeId" value="@skill.EmployeeID" />
                                                <input type="hidden" name="skillId" value="@skill.SkillID" />
                                                <select name="supervisorRatedSkillLevel" class="form-select form-select-sm me-2" required>
                                                    <option value="">Select</option>
                                                    @for (int i = 1; i <= 5; i++)
                                                    {
                                                        <option value="@i">@i</option>
                                                    }
                                                </select>
                                                <button type="submit" class="btn btn-success btn-sm">Save</button>
                                            </form>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    }
                    else
                    {
                        <div class="alert alert-info text-center">No pending skills for approval.</div>
                    }
                    <div class="text-center mt-4">
                        <form asp-action="Logout" asp-controller="Account" method="post" style="display: inline;">
                            <button type="submit" class="btn btn-danger">
                                <i class="fas fa-sign-out-alt me-2"></i>Logout
                            </button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
@* 
[Authorize(Roles = "Admin")]
public IActionResult AdminView()
{
    return View();
} *@
<!-- This is the end of the AdminView.cshtml file -->